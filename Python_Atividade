#Dupla: Pedro Gabriel e Ryan Vinicius

nome = input("Qual o seu nome: ") #Inserir o nome do usuário.
anona = input("Qual o ano do seu Nascimento? ") #Inserir o ano de nascimento do usuário.
anoat = input("Qual o ano atual? ") #Inserir o ano atual.
soma = int(anona) + int(anoat) #A soma entre o ano de nascimento e o ano atual.
idade = int(anoat) - int(anona)
print("Idade:", idade)
print("A soma entre os dois anos é igual a:",soma) #Apresenta na tela o resultado da soma.

print("==============================================================================================================================================================================================================")
#^Só para sepração entre os dois "Problemas".
print("Um grupo de alunos deveriam estar na escola às 13:00, mas devido a um atraso no metro chegaram às 14:27.\nQuanto tempo se atrasaram?") #O problema..
a = 13*60 #Transformação de horas para minutos.

print("13 x 60 = 780")
print("14 x 60 = 840 + 27 = 867  ")
print("867-780= 87 ")
print("87/60= 1.45")

b = (14*60) + 27 #Transformação de horas para minutos.
c = b - a #Subitração da variavél "a" e "b", para descobrir a diferença.
d = c/60 #Tranformação de minutos para horas.
d = str(d).replace('.',' hora e ') #Troca os ".", para o ":".
print("R: Os alunos se atrasaram",d,"minutos.") #Mostra na tela a resposta do problema acima.
